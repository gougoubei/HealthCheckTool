name: Build Mac App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4  # 已升级到 v4

    - name: Set up Python
      uses: actions/setup-python@v5 # 已升级到 v5
      with:
        python-version: "3.9"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build with PyInstaller
      run: |
        # 你的应用名字
        pyinstaller --noconsole --onefile --name="体检转换工具" main.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4 # 🔴 关键修复：这里改成了 v4
      with:
        name: Mac-App-Download
        path: dist/体检转换工具
        # v4 版本默认会把你的文件压缩成 zip 下载，非常方便
