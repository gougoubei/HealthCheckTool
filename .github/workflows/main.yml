name: Build Mac App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    # 🔴 核心修改：指定使用 macOS 12 (最稳定的旧版环境)
    runs-on: macos-12

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        # 强制使用 x64 架构，确保生成的软件是 Intel 版的
        # 这样你的 Intel Mac 和朋友的 M1 Mac 都能跑
        architecture: "x64"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build with PyInstaller
      run: |
        # 最简单的打包命令
        pyinstaller --noconsole --onefile --name="体检转换工具" main.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mac-App-Final
        path: dist/体检转换工具
