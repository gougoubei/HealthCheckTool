name: Build Mac App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    # 使用最新的 macOS 环境
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        # 🔴 关键修改：强制使用 x64 (Intel) 架构的 Python
        # 这样 GitHub 会通过 Rosetta 模拟运行 Intel 环境
        architecture: "x64"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build with PyInstaller
      run: |
        # 回归最简单的打包命令，去掉复杂的架构参数
        # 因为 Python 本身已经是 Intel 版的了，生成的软件自然也是 Intel 版
        pyinstaller --noconsole --onefile --name="体检转换工具" main.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mac-App-Intel
        path: dist/体检转换工具
